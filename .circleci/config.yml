version: 2.1
orbs:
  node: circleci/node@5
jobs:
  hello-job:
    executor:
      name: node/default
      tag: '20.11.0'
    steps:
      - checkout
      - run: echo "This should only be run when run-ci label is added on the PR"
workflows:
  sample-workflow:
    jobs:
      - hello-job